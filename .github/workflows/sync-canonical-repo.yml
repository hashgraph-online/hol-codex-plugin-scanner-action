name: Sync Canonical Action Repo (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    if: github.repository == 'hashgraph-online/hol-codex-plugin-scanner-action'
    runs-on: ubuntu-latest
    steps:
      - name: Announce deprecated direction
        run: |
          echo "Canonical publishing now runs from hashgraph-online/ai-plugin-scanner-action." >> "$GITHUB_STEP_SUMMARY"
          echo "This legacy repository no longer syncs changes into the canonical repository." >> "$GITHUB_STEP_SUMMARY"
